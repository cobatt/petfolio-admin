--- a/admin.html
+++ b/admin.html
@@ -848,29 +848,37 @@
     <!-- alias 히스토리 -->
     <div class="card" id="alias-history-card">
       <div class="card-header">
         <div class="card-title">📜 alias 변경 히스토리</div>
         <div style="display:flex;gap:8px;align-items:center">
           <span id="alias-history-count" style="font-size:12px;color:#6B7570"></span>
-          <button class="btn btn-outline" onclick="loadAliasHistory()">불러오기 / 새로고침</button>
+          <button class="btn btn-outline" onclick="loadAliasHistory(1)">불러오기 / 새로고침</button>
         </div>
       </div>
       <div style="font-size:12px;color:#6B7570;padding:0 20px 12px">
-        원재료 이름 변경 · 병합 · AI 분리 시 자동 기록되는 매핑 이력. 최신순 50개.
-        rebuild 시 본 테이블의 <code>original_name</code> 으로 옛 라벨도 누락 없이 매칭됩니다.
+        원재료 이름 변경 · 병합 · AI 분리 시 자동 기록되는 매핑 이력.
+        rebuild 시 <code>original_name</code> 으로 옛 라벨도 누락 없이 매칭됩니다.
+        불필요한 항목은 삭제하여 rebuild 매핑에서 제외하세요.
       </div>
-      <div id="alias-history-list" style="display:none">
+      <div style="padding:0 20px 12px;display:flex;gap:8px">
+        <input id="alias-search-input" type="text" placeholder="원본명 또는 현재명 검색..."
+          style="flex:1;padding:6px 10px;border:1px solid #D1D5DB;border-radius:6px;font-size:13px"
+          oninput="loadAliasHistory(1)">
+      </div>
+      <div id="alias-history-list">
         <div class="table-wrap">
           <table>
             <thead>
               <tr>
                 <th>원본명 (original_name)</th>
                 <th style="width:32px;text-align:center">→</th>
                 <th>현재명 (current)</th>
                 <th style="width:160px">기록 시각</th>
+                <th style="width:60px"></th>
               </tr>
             </thead>
             <tbody id="alias-history-tbody"></tbody>
           </table>
         </div>
+        <div id="alias-history-pagination" style="display:flex;gap:4px;padding:12px 20px;justify-content:center"></div>
       </div>
     </div>

@@ -3697,52 +3697,82 @@
 // ── alias 변경 히스토리 ─────────────────────────────────────────
 function _fmtAliasDate(iso) {
   if (!iso) return '';
   const d = new Date(iso);
-  if (isNaN(d.getTime())) return iso;
-  const yy = d.getFullYear();
-  const mm = String(d.getMonth() + 1).padStart(2, '0');
-  const dd = String(d.getDate()).padStart(2, '0');
-  const hh = String(d.getHours()).padStart(2, '0');
-  const mi = String(d.getMinutes()).padStart(2, '0');
-  return `${yy}-${mm}-${dd} ${hh}:${mi}`;
+  const pad = n => String(n).padStart(2, '0');
+  return `${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())} ${pad(d.getHours())}:${pad(d.getMinutes())}`;
 }
-
 function _escAlias(s) {
-  if (s == null) return '';
-  return String(s)
-    .replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;')
-    .replace(/"/g, '&quot;').replace(/'/g, '&#39;');
+  return String(s||'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
 }

-async function loadAliasHistory() {
-  const listEl = document.getElementById('alias-history-list');
+const ALIAS_PAGE_SIZE = 30;
+let _aliasCurrentPage = 1;
+let _aliasTotalCount = 0;
+
+async function loadAliasHistory(page = 1) {
+  _aliasCurrentPage = page;
+  const q = (document.getElementById('alias-search-input')?.value || '').trim();
+  const offset = (page - 1) * ALIAS_PAGE_SIZE;
+  const countEl = document.getElementById('alias-history-count');
   const tbody = document.getElementById('alias-history-tbody');
-  const countEl = document.getElementById('alias-history-count');
+  const pagEl = document.getElementById('alias-history-pagination');
+
+  countEl.textContent = '불러오는 중...';
+  tbody.innerHTML = '';
+  pagEl.innerHTML = '';
+
   try {
-    countEl.textContent = '로딩 중...';
-    const r = await fetch(`${API}/admin/ingredients/aliases?limit=50`);
-    if (!r.ok) {
-      countEl.textContent = `오류 (${r.status})`;
-      return;
-    }
+    const url = `${API}/admin/ingredients/aliases?limit=${ALIAS_PAGE_SIZE}&offset=${offset}${q ? '&q='+encodeURIComponent(q) : ''}`;
+    const r = await fetch(url);
+    if (!r.ok) throw new Error(r.status);
     const items = await r.json();
-    countEl.textContent = `${items.length}건`;
+
+    _aliasTotalCount = items.length < ALIAS_PAGE_SIZE
+      ? offset + items.length
+      : offset + items.length + 1;
+
+    countEl.textContent = items.length ? `${offset+1}–${offset+items.length}건` : '0건';
+
     if (!items.length) {
-      tbody.innerHTML = `<tr><td colspan="4" style="padding:16px;text-align:center;color:#C2C9C5">아직 기록된 변경 이력이 없어요.</td></tr>`;
+      tbody.innerHTML = '<tr><td colspan="5" style="text-align:center;color:#9CA3AF;padding:20px">검색 결과가 없습니다.</td></tr>';
     } else {
-      tbody.innerHTML = items.map(a => `
+      tbody.innerHTML = items.map(item => `
         <tr>
-          <td style="font-family:'Courier New',monospace">${_escAlias(a.original_name)}</td>
-          <td style="text-align:center;color:#4A7C59">→</td>
-          <td>${a.current_name ? _escAlias(a.current_name) : '<span style="color:#C2C9C5">(삭제됨)</span>'}</td>
-          <td style="font-size:12px;color:#6B7570">${_fmtAliasDate(a.created_at)}</td>
+          <td style="font-family:monospace;font-size:12px">${_escAlias(item.original_name)}</td>
+          <td style="text-align:center;color:#9CA3AF">→</td>
+          <td style="font-family:monospace;font-size:12px">${item.current_name ? _escAlias(item.current_name) : '<span style="color:#9CA3AF">(삭제됨)</span>'}</td>
+          <td style="font-size:12px;color:#6B7570">${_fmtAliasDate(item.created_at)}</td>
+          <td style="text-align:center">
+            <button class="btn btn-danger btn-sm" onclick="deleteAlias('${item.id}')">삭제</button>
+          </td>
         </tr>
       `).join('');
     }
-    listEl.style.display = 'block';
+
+    const hasPrev = page > 1;
+    const hasNext = items.length === ALIAS_PAGE_SIZE;
+    if (hasPrev || hasNext) {
+      pagEl.innerHTML = `
+        ${hasPrev ? `<button class="btn btn-outline btn-sm" onclick="loadAliasHistory(${page-1})">← 이전</button>` : ''}
+        <span style="padding:4px 10px;font-size:13px;color:#6B7570">${page}페이지</span>
+        ${hasNext ? `<button class="btn btn-outline btn-sm" onclick="loadAliasHistory(${page+1})">다음 →</button>` : ''}
+      `;
+    }
   } catch(e) {
-    console.error(e);
     countEl.textContent = '오류';
+    tbody.innerHTML = `<tr><td colspan="5" style="color:red;padding:20px">${e.message}</td></tr>`;
+  }
+}
+
+async function deleteAlias(id) {
+  if (!confirm('이 alias 항목을 삭제하시겠습니까?\nrebuild 시 이 매핑은 더 이상 적용되지 않습니다.')) return;
+  try {
+    const r = await fetch(`${API}/admin/ingredients/aliases/${id}`, { method: 'DELETE' });
+    if (!r.ok) throw new Error(r.status);
+    loadAliasHistory(_aliasCurrentPage);
+  } catch(e) {
+    alert('삭제 실패: ' + e.message);
+  }
+}
